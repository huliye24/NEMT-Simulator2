# Cloudflare Pages 项目配置
# 部署命令: wrangler pages deploy web/dist --project-name=nemt-simulator

# 构建设置
build_command: npm run build
build_output_dir: web/dist

# 路由配置 (可选)
# routes:
#   - pattern: nemt.example.com
#     zone_name: example.com
